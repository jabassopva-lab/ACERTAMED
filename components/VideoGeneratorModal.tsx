
import React from 'react';

// Este componente foi descontinuado e removido do projeto.
// Ele foi mantido apenas para evitar erros de compilação em arquivos que ainda o referenciem.
const VideoGeneratorModal: React.FC<any> = () => {
  return null;
};

export default VideoGeneratorModal;
